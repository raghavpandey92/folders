#include<stdio.h>
int main(){
   // int n ;
    //printf("enter the year:");
    //scanf("%d",&n);
   // if(n%4==0)
    //printf("the given year is leap year");
   // else
    //printf("the given no. is not a leap year");

    int x;
    printf("enter a no:");
    scanf("%d",&x);
    if(x>0)
         printf("the given no is positive");
        else if(x==0)printf("the given no. is neutral");
    else printf("the given no. is negative");

}
